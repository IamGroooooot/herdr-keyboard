{
  "name": "herdr-keyboard",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs && npm run test:types",
    "typecheck": "tsc -p tsconfig.test.json",
    "test:types": "tsx --test test/*.test.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "effect": "3.22.2"
  },
  "devDependencies": {
    "@types/node": "20.19.43",
    "tsx": "4.23.13",
    "typescript": "7.0.2"
  }
}
